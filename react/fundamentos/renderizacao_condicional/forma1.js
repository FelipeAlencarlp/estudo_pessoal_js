// IF simples (fora do JSX)
function App() {
    const logado = true;

    if (logado) {
        return <h1>Bem-vindo!</h1>;
    }

    return <h1>Faça login</h1>;
}

// Usado para quando quero trocar toda a tela